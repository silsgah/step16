import { Keypair, Transaction } from "stellar-base";
import { ServerApi } from "./server_api";
export declare namespace Utils {
    function buildChallengeTx(serverKeypair: Keypair, clientAccountID: string, anchorName: string, timeout?: number, networkPassphrase?: string): string;
    function readChallengeTx(challengeTx: string, serverAccountId: string, networkPassphrase?: string): {
        tx: Transaction;
        clientAccountID: string;
    };
    function verifyChallengeTxThreshold(challengeTx: string, serverAccountID: string, networkPassphrase: string, threshold: number, signerSummary: ServerApi.AccountRecordSigners[]): string[];
    function verifyChallengeTxSigners(challengeTx: string, serverAccountID: string, networkPassphrase: string, signers: string[]): string[];
    function verifyChallengeTx(challengeTx: string, serverAccountId: string, networkPassphrase?: string): boolean;
    function verifyTxSignedBy(transaction: Transaction, accountID: string): boolean;
    function gatherTxSigners(transaction: Transaction, signers: string[]): string[];
}
